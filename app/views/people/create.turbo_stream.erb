<%= turbo_stream.prepend "todos" do %>
    <%= render "person", person: @person %>
<% end %>

<%= turbo_stream.replace "#{dom_id(Person.new)}_form" do %>
    <%= render "form", person: Person.new %>
<% end %>